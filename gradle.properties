# Build performance. Caching reuses task outputs across builds (CI persists ~/.gradle via
# gradle/actions/setup-gradle), parallel builds independent subprojects at once.
org.gradle.caching=true
org.gradle.parallel=true
org.gradle.configuration-cache=true
